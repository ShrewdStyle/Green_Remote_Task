import React, { Fragment } from "react";
import LoginForm from "../Layout/Login/LoginForm";

const Login = () => {
  return (
    <Fragment>
      <LoginForm />
    </Fragment>
  );
};

export default Login;
